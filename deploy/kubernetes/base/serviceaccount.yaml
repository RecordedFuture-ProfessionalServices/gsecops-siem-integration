apiVersion: v1
kind: ServiceAccount
metadata:
  name: rf-risklist-ingest
# The job talks only to the Recorded Future and Google SecOps APIs, so it needs
# no Kubernetes RBAC. On GKE this account is bound to a Google service account
# instead - see overlays/gke-workload-identity.
automountServiceAccountToken: false
